# Required — change this to a secure random string (min 32 characters)
JWT_SECRET=change-me-to-a-secure-random-64-char-hex-string

# Optional
PORT=3000
NODE_ENV=development
DB_PATH=.poker-data/poker.db
NEXT_PUBLIC_APP_URL=http://localhost:3000
